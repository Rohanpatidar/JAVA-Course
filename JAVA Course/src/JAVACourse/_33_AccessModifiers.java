package JAVACourse;

/**
 * The _33_AccessModifiers class demonstrates the use of access modifiers in Java.
 * Specifically, it includes a protected member variable, showcasing the protected
 * access level.
 * <p>
 * Protected members can be accessed within the same package and by subclasses,
 * providing controlled access that balances encapsulation and flexibility.
 */
public class _33_AccessModifiers {
    protected int marks = 6;
}


// Public    = Access form everywhere
// Private   = Access only from same class
// Protected = It not access outside the package but if extends
//             the subclass then we access protected
// Default   = It can be access only from same package

/*


Same Class


 */